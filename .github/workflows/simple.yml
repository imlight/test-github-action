name: Shell commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: ecccho "Hello World"
      - name: Multipleine script
        run: |
          node -v
          npm -v

